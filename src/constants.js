const APPLY = 'apply';
const ADD = 'add';
const SUBTRACT = 'subtract';
const MULTIPLY = 'multiply';
const DIVIDE = 'divide';

const ERROR_MISSING_APPLY =
  'The last line of a list of a instructions needs to be "apply" and then a value';

module.exports = {
  APPLY,
  ADD,
  SUBTRACT,
  MULTIPLY,
  DIVIDE,
  ERROR_MISSING_APPLY,
};
